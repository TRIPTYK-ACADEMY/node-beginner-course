/**
 * Faire un serveur Http servant des réponses en JSON
 * 📌 Faire la route GET /users/ qui renvoie le JSON de tous les utilisateurs
 * 📌 Faire la route GET /users/?id=1 qui renvoie un utilisateur avec un id spécifique
 * 📌 Faire la route POST /users qui ajoute un utilisateur dans le JSON
 * 📌 Gérer les NOT FOUND
 */
